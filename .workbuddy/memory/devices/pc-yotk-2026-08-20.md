# pc-yotk 2026-08-20

## 修复：覆盖对端工作事故 + 建立同步铁律（commit ce329d3 / 8e8b867）

### 事故
用户指出我上一轮在本地旧版上工作，把对端(remote-1)的工作覆盖了。排查确认：
- 对端做了：多产品首页（科幻中国象棋/汉字解锁/霓虹突袭/ChatHub/魔鬼代言人）、两个完整游戏(games/neon-xiangqi、games/neon-strike)、汉字 demo(apps/hanzi-demo)，并部署到 Pages production（快照 5e398815/source 438ee23、9fbd0e23/source df7ac87 等）
- 对端的提交**从未 push 到 GitHub**（remote main 只有我的提交），只存在于 Pages 部署快照 → 我 push+部署时把线上覆盖了，游戏"消失"
- 我处理邮件时忽略了"首页已改回四款产品"这条关键信息，未先 pull/核对线上

### 恢复方案（成功）
1. wrangler pages deployment list 找到对端部署 → API(oauth token) 拿文件清单 → 从部署 URL 用 curl -L 下载全部 51 个文件（注意：Pages 对 .html 返回 308 重定向到无后缀，node https 不跟重定向会失败，curl -L 解决）
2. git diff 9abefae 定位对端实质改动：仅 index.html、products.html（其他页面 0 实质改动）
3. 合并：其余页面 checkout 8ac09bc（我的双语版）+ 脚本批量补导航 games/hanzi 项与页脚 3 产品链接（注意 CRLF：正则用 \r?\n；紧凑页脚无换行）
4. 词典 en.core.js 补 nav.games/nav.hanzi/footer.xiangqi|neon|hanzi + index/products 三产品卡翻译
5. 魔鬼代言人保持"已上线/立即使用"（对端版标回孵化中被我改回）
6. 验证 941 个 data-i18n 引用零缺失 → 部署 → 全站 200 → push

### 教训（已写进 SYNC.md「同步铁律」）
- GitHub main 是唯一事实源；改完必 push、开工前必 pull、部署前核对线上、禁止覆盖对方工作、push 失败必须告知
- 处理对端邮件时，先核对线上/仓库再动手

## 游戏/应用页接入双语（commit c865ae0）

- 用户反馈"象棋/射击/汉字解锁没翻译"（上一轮只做了主站 18 页，漏了 games/ 和 apps/）
- 三个页面全部接入 i18n：
  - neon-xiangqi：英文直出改中文直出 + 英文词典；规则弹窗/侧栏/按钮全双语；main.js 动态文本（走棋/胜负/思考/吃子/楚河汉界/全屏/音效按钮）改 AllSoftI18n.t(key, zh) 取词，T 辅助函数注入文件顶部；语言切换靠监听 i18n:change 刷新（棋盘 canvas 每帧重绘自动生效）
  - neon-strike：925 行单文件，菜单/暂停/结算/排行榜/操作说明双语 + JS 动态文本（暂无记录/新纪录/得分）；注意内联 JS 替换时源字符串带引号要整串替换，否则引号残留报语法错（踩坑一次）
  - hanzi-demo：标题/按钮/说明/加载失败提示双语
- i18n.js 增强：暴露 AllSoftI18n.t(key, fallback)/isEn()，toggle 后 dispatch window CustomEvent('i18n:change')
- 页面右上角手工放 #langSwitch 按钮（游戏页无导航，i18n.js 检测到已存在即绑定）
- 验证：21 页面 1020 data-i18n 引用零缺失；部署后 CDN 缓存延迟约 5-10 秒（部署快照 URL 立即正确，production 域名稍后）

## 夜间主题切换（commit c6b3570）

- 用户需求：护眼夜间主题
- 实现：CSS 变量体系（已存在）加 html.dark 暗色变量集（深蓝灰 #0f172a 系，低对比护眼）；卡片/移动导航/FAQ 按钮/code 背景从硬编码 #fff 改为 var(--c-bg) 驱动（浅色不变、暗色自动变深）；site-header 半透明背景/hero 渐变/代码块加 dark 覆盖；新增 --c-header-bg/--c-hero-1 变量
- 新增 js/theme.js：导航栏自动注入 🌙/☀️ 按钮（复用 .nav-lang-btn 样式），localStorage allsoft_theme 记忆，未设置时跟随系统 prefers-color-scheme
- 15 个主站页面引入 theme.js（批量在 i18n.js 后插入）；hanzi-demo 内联 html.dark 样式 + 手工 themeSwitch 按钮；游戏页本身暗色不动
- 验证：theme.js 语法 OK、页面引用齐全、线上 CSS/JS/页面全部生效

## 主题按钮无效修复：缓存（commit 02adac2）

- 用户反馈"按钮没变化"→ 根因：_headers 对 *.css 设 Cache-Control max-age=31536000（1 年），页面引用 css/style.css?v=2 未 bump → 浏览器/CDN 用旧 CSS（无 html.dark 样式），点击 🌙 只是加了 html.dark 类但样式不存在
- 修复：全站 17 页 css?v=2→?v=3；顺带 18 页 js/i18n.js 加 ?v=2（上轮 i18n.js 新增 t()/i18n:change 也受缓存影响）
- 项目惯例（重要）：_headers 对 css/js 1 年强缓存，**改 css/js 后必须 bump 引用版本号**，否则线上永远旧版；改完 curl 线上带 ?v= 校验

## 夜间主题不彻底修复（commit b3594c1）

- 用户反馈"不够彻底，有的区域没切换"→ 全量排查 css + HTML 内联：
  - 7 处标题用 var(--c-bg-dark) 作文字色（hero h1/section-header/plan-price/blog-card h3/article h1-h3），暗色下变黑不可见 → 改 var(--c-text)（浅色下几乎无差异）
  - page-header 渐变硬编码 → 变量（页头区跟随暗色）；blog-img/feature-card icon 渐变 → 变量
  - tag-live/new/beta 产品标签加 html.dark 深色版
  - contact-form 输入框补 background/color
  - faq.html 4 处内联 color:var(--c-bg-dark) → var(--c-text)
  - hanzi-demo 右上角固定按钮 dark 覆盖；admin 内联 dark 样式 + 引入 theme.js
- 教训：**var(--c-bg-dark) 是背景色变量，绝不能用做文字色**（暗色下文字变黑）；项目里原本大量这样用
- CSS v3→v4 破缓存；验证线上全部生效

## 主题缓存死结彻底解决：CSS 文件名版本化（commit 093c23e）

- 用户仍反馈"没有改变 / 深色字看不清"→ 排查确认根因：Cloudflare Pages CDN 按路径缓存 css，**忽略 ?v= query**，_headers 又设 1 年缓存 → 无论怎么 bump ?v=，CDN/用户都拿旧 css（实测 production 返回旧内容，部署快照 .pages.dev 是新内容）
- 解法：css/style.css 复制为 **css/style.v5.css（新文件名=新 URL=必然回源）**，全站 17 页引用切换
- **项目新规（重要）**：改 css 后必须同步复制到 style.vN.css 并切换全站引用；js 同理（js/xxx.vN.js 或依赖内容更新）；OAuth token 无 purge_cache 权限，不要指望清缓存
- 附带：暗色对比度提升（color-scheme: dark 原生控件反色 + --c-text/#e8edf5、--c-text-light/#a6b4c8 提亮）
- 验证：style.v5.css 新路径线上立即返回最新内容 ✓

## 后台流量图表升级为 SVG 折线图（commit 3c4f774/07af5e0）

- 根因：原柱状图 filter(pv>0) 后若只剩 1 天数据，flex:1 的 .bar 撑满 200px 图表区，呈现大蓝块覆盖（"卡不到曲线被覆盖"）
- 修复：bar-chart → line-chart（SVG 折线图，30 天全部点连线，0 值点画在基线，浏览器原生 title hover tooltip）
- 暗色：line 变浅蓝、pt 描边深色、grid/axis 跟随暗色变量
- 验证：浅色+暗色各截图确认（30 天 0 基线 + 8-19 174 尖峰趋势清晰）
- 顺手：theme.js 加 ?theme=dark/light URL 参数（方便分享深色链接 + 截图调试；URL > localStorage > 系统偏好）
- theme.js 加 ?v=2 破 1 年 CDN 缓存

## 踩坑：git add -A 误提交 Edge 浏览器数据
- Edge headless 截图用了 --user-data-dir=_edgedata，目录几百个缓存文件，加上调试截图 .png + _seed.html，git add -A 一次性全部入仓
- 修复：git rm --cached + amend + force push（仓库私有单人）；.gitignore 加 _edgedata/ + _seed.html + admin_*.png
- 教训：**调试产物必须先列 .gitignore 再开工**；git add -A 前 `git status` 检查

## 后台流量图表升级为 SVG 折线图（commit 3c4f774/07af5e0）

- 根因：原柱状图 filter(pv>0) 后若只剩 1 天数据，flex:1 的 .bar 撑满 200px 图表区，呈现大蓝块覆盖（"卡不到曲线被覆盖"）
- 修复：bar-chart → line-chart（SVG 折线图，30 天全部点连线，0 值点画在基线，浏览器原生 title hover tooltip）
- 暗色：line 变浅蓝、pt 描边深色、grid/axis 跟随暗色变量
- 验证：浅色+暗色各截图确认（30 天 0 基线 + 8-19 174 尖峰趋势清晰）
- 顺手：theme.js 加 ?theme=dark/light URL 参数（方便分享深色链接 + 截图调试；URL > localStorage > 系统偏好）
- theme.js 加 ?v=2 破 1 年 CDN 缓存

## 踩坑：git add -A 误提交 Edge 浏览器数据
- Edge headless 截图用了 --user-data-dir=_edgedata，目录几百个缓存文件，加上调试截图 .png + _seed.html，git add -A 一次性全部入仓
- 修复：git rm --cached + amend + force push（仓库私有单人）；.gitignore 加 _edgedata/ + _seed.html + admin_*.png
- 教训：**调试产物必须先列 .gitignore 再开工**；git add -A 前 `git status` 检查

## 词典 CDN 缓存根因修复（commit 995c20f）—— 重要项目规范

- 用户反馈"象棋/汉字解锁/霓虹突袭介绍段没翻译"→ 根因：**js/lang/en.core.js 无版本号，被 CDN 按路径 1 年缓存**，用户浏览器拿到最初发布版本，后来新增的 key（nav.games、index.card.xiangqi/hanzi/neon.*、xq.*、st.*、admin.*、chat.* 等）全部缺失 → 这些段切英文仍中文（导航能翻译因为旧词典里有 nav.*）
- 修复：en.core.js→en.core.v2.js、en.blog.js→en.blog.v2.js，全站 20/3 页切换引用
- **本项目缓存规范（铁律）**：_headers 对 css/js 设 1 年缓存，Cloudflare Pages CDN 按路径缓存且忽略 ?v= query。**改任何 css/js/词典文件后，必须换文件名版本（style.vN.css / xxx.vN.js）并全站切换引用**，?v= 参数无效，清缓存 API 无权限
- 当前版本化清单：css/style.v5.css、js/i18n.v2.js、js/lang/en.core.v2.js、js/lang/en.blog.v2.js、js/theme.js?v=2
- 验证：线上 en.core.v2.js 含象棋/汉字/霓虹/admin/chat key ✓；首页引用全版本化 ✓

## admin 数据页从前端隐藏（commit 7684b2c）

- 用户需求：admin 数据页 https://allsoft.asia/admin 从前端隐藏，仅输网址可访问。
- 操作：移除全站 17 个 html 页面导航中的「网站数据」入口（`<li><a href="/data">`），含 index/about/404/admin/blog/chat/contact/faq/pricing/privacy/terms/products 及 blog/、products/ 子页。
- 保留：_redirects 中 `/data /admin 301` 重定向不变 → 直接输 /data 或 /admin 均可访问；admin.html 自身已有 noindex。
- 验证：全站 grep 无 href="/data" 残留、js 无动态 admin 链接；pull 后 push 成功（GitHub 443 墙本次未发作）。
- 注意：改动仅为导航链接删除，页面文件本身未动；导航栏其余项未受影响。

## 全站描述文案更新至五款已上线软件（commit 674e69e，push 待重试）

- 背景：新增霓虹突袭/科幻象棋/汉字解锁等游戏软件后，全站介绍文案滞后。
- 更新点：
  1. index hero lead：魔鬼代言人从「孵化中」→「已上线」，并补「全部已上线」措辞
  2. about why.d2：「目前集合里有两款」→「已有五款全部上线」+ 五个链接
  3. about next.d：同步「五款软件已全部上线」
  4. faq q1.a + JSON-LD：「收录两款」→「五款已上线软件」；删掉过时「AI工具箱」措辞
  5. 英文词典 en.core.v2.js → en.core.v3.js（CDN 铁律：改词典必须换文件名），3 处 key 同步，全站 20 页切换引用；vm 校验 665 keys 无丢失
- 部署：wrangler pages deploy 成功，线上已验证首页/about/FAQ/词典 v3 全部生效。
- ⚠️ push 被 GitHub 443 墙拦截（多次重试失败），已通过总线 send id=25 告知对端，待网络恢复重推。

## 09:40 全站身份更新：OPC/一人公司 → 北京筑傲科技软件开发部门
- 用户指令：不再用 OPC 一人公司作网站身份描述，开发者主体改为北京筑傲科技有限公司（Beijing Zhuao Science And Technology Co., Ltd）旗下软件开发部门，中英文同步更新。
- 改动（脚本批量，20 文件 154 处替换）：
  1. 全站 footer.desc、index hero（badge/title/lead/btnAbout）、meta title/description/keywords/og/tw、JSON-LD（description + founder→parentOrganization）、about 全页、faq q1.a、products 全页、llms.txt
  2. 英文词典 en.core.js / v2 / v3 三版同步（footer、hero、about、products、faq、contact 等 30+ key），vm 校验各 665 keys 无丢失
  3. 保留项：OPC 博客文章正文与卡片文案（blog.p2.desc/index.blog2.desc/post.opc.*）不动，仅改文章结尾「我们也在用这种方式运营 allsoft.asia」→ 公司软件开发部门运营
- 校验：HTML 无「工作室/独立开发者/一人独立」残留；JS 语法 OK。
- commit efe142c 已 push 成功（本次 443 墙未拦，连带 07:37 积压的 674e69e + ffa01c7 一并推送，主线已追平）。
- 追加：线上 en.core.v3.js 被 CDN 缓存（旧文案）→ 换名 en.core.v4.js（commit edabfa5）全站 20 页切换，重新部署后线上校验通过（v4 词典/首页引用/中英文全生效）。

## 09:55 移除全站 OPC/一人公司/个人创业痕迹（用户二轮指令）
- 用户要求：不再显示任何 OPC/一人公司/个人创业者迹象，含文章内容。
- 动作（commit 4caea87）：
  1. 删除 blog/opc-one-person-company.html 整篇文章
  2. 首页 + blog 列表删 OPC 卡片（blog-grid 改 auto-fit 自适应 2 卡）
  3. 词典删 index.blog2.*/blog.p2.* 与 post.opc.* 全部 key（en.core 656 keys、en.blog 重排）
  4. 换名破 CDN：en.core.v4→v5、en.blog.v2→v3，全站 19 页切换引用
  5. free-resources 标题「零成本创业」→「零成本起步」（中英同步），blog meta/sub 去「创业指南」
  6. sitemap/llms.txt 删 OPC 索引；_redirects 加 /blog/opc-one-person-company → /blog 301
  7. team.py 示例文案改中性
- 部署 6ae11489 后线上校验：index/blog 无 OPC 残留、OPC 文章 301→/blog、v5 词典生效、llms/sitemap 干净。

## 10:58 邮件通道诊断（用户反馈：对端好像没收到邮件）
- 查 agent-mail：对端 remote-1（mmfd8440@agent.qq.com）8-19 15:45 发过「重要」邮件（已接入总线、改过首页、3 个 commit 368e6e9/9454fa5/df7ac87 因无 push 凭据未进 GitHub main），我此前一直未回复。
- 我 8-19 13:29/13:52 发的「乱码已修复」两封对端未确认收到（疑似丢失）。
- 已重发一封中文回信（queued）逐条回应：总线 id=25/26/27 请它 poll、旧版首页已被公司身份+五款新版本取代无需再推、乱码修复以新信为准、请回信确认通道。
- 历史退信：330101365861@agent.qq.com / 13718272791@agent.qq.com / 330101365861 均不存在（8-19 已确认，勿再用）。

## 16:02 值守轮次（自动化）
- git pull --rebase：Already up to date。
- poll：无发给本机的消息；任务板无 open 任务；在线 pc-yotk / remote-1（最后活跃 03:11）。
- 唯一动作：上一轮 14:45 的共享日志改动未提交 → commit d41d10a 并 push 成功（8c85d53..d41d10a）。
- 无抢单、无文件锁需求，本轮结束。
